##R001##
##10##

# Define con tus palabras el concepto de una closure colócalo en una cadena y usa print()

concepto_closure = """
Un closure es una función que tiene acceso a las variables de la
función externa que la contiene, incluso después de que la función 
externa haya terminado de ejecutarse. La función interna captura 
esas variables, por eso se llama closure, que viene de cerrar o 
encerrar.
"""
print("\nEjercicio 1 (R001)", concepto_closure, sep="\n")
